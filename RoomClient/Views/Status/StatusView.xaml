<UserControl x:Class="RoomClient.Views.Status.StatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="180"
             d:DesignWidth="520">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock FontSize="15" FontWeight="SemiBold" Foreground="White" Text="Room Status" />
            <TextBlock Margin="0,2,0,8" FontSize="11" Foreground="#FF94A3B8" Text="Informasi ruangan Anda." />
        </StackPanel>

        <!-- Sisa waktu sesi -->
        <Border Grid.Row="1" Margin="0,0,0,8" Padding="12" Background="#FF111827" CornerRadius="14">
            <StackPanel>
                <TextBlock FontSize="11" Foreground="#FF94A3B8" Text="Sisa Waktu Sesi" />
                <TextBlock Margin="0,2,0,0"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="#FFFACC15"
                           Text="{Binding Player.RemainingTimeText, FallbackValue=--:--}" />
            </StackPanel>
        </Border>

        <!-- Room yang sedang dipakai client ini saja -->
        <Border Grid.Row="2" Padding="10" Background="#FF111827" CornerRadius="14">
            <StackPanel DataContext="{Binding Status.SelectedRoom}">
                <DockPanel LastChildFill="False">
                    <TextBlock FontSize="14" FontWeight="SemiBold" Foreground="White" Text="{Binding RoomNumber}" />
                    <Border Margin="10,0,0,0" Padding="7,2" Background="#FF1D4ED8" CornerRadius="999" DockPanel.Dock="Right">
                        <TextBlock FontSize="10" FontWeight="SemiBold" Foreground="White" Text="{Binding RoomStatus}" />
                    </Border>
                </DockPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>