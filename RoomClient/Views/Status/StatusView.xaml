<UserControl x:Class="RoomClient.Views.Status.StatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="420"
             d:DesignWidth="520">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="White" Text="Room Status" />
            <TextBlock Margin="0,4,0,8" FontSize="12" Foreground="#FF94A3B8" Text="Data rooms dari API lokal." />
        </StackPanel>

        <Border Grid.Row="1" Margin="0,0,0,10" Padding="12" Background="#FF111827" CornerRadius="14">
            <StackPanel>
                <TextBlock FontSize="12" Foreground="#FF94A3B8" Text="Status" />
                <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="White" Text="{Binding Status.StatusMessage}" TextWrapping="Wrap" />
                <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="#FFF87171" Text="{Binding Status.ErrorMessage}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Padding="12" Background="#FF111827" CornerRadius="14">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Status.Rooms}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,10" Padding="12" Background="#FF0B1220" CornerRadius="12">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <TextBlock FontSize="15" FontWeight="SemiBold" Foreground="White" Text="{Binding RoomNumber}" />
                                        <Border Margin="10,0,0,0" Padding="8,3" Background="#FF1D4ED8" CornerRadius="999" DockPanel.Dock="Right">
                                            <TextBlock FontSize="11" FontWeight="SemiBold" Foreground="White" Text="{Binding RoomStatus}" />
                                        </Border>
                                    </DockPanel>

                                    <TextBlock Margin="0,6,0,0" FontSize="12" Foreground="#FF94A3B8" Text="{Binding ClientIp, StringFormat=Client IP: {0}}" />
                                    <TextBlock Margin="0,2,0,0" FontSize="12" Foreground="#FF94A3B8" Text="{Binding RoomId, StringFormat=Room ID: {0}}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Border Grid.Row="3" Margin="0,10,0,0" Padding="12" Background="#FF111827" CornerRadius="14">
            <StackPanel>
                <TextBlock FontSize="12" Foreground="#FF94A3B8" Text="WebSocket Log" />
                <ItemsControl Margin="0,8,0,0" ItemsSource="{Binding Status.EventLogs}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Margin="0,0,0,4" FontSize="12" Foreground="White" Text="{Binding}" TextWrapping="Wrap" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
